该压缩程序采用哈夫曼树这种无损压缩算法，可对任何不是非常大的文件进行压缩，对于文本类文件压缩效果较好，大约可压缩到原文件75%左右。(其实很弱......只是作者自娱自乐罢了)

使用方法非常简单，将对应cpp文件编译出exe文件，然后在当前目录下打开命令行，输入命令：

compress + 空格 + 文件名 ， 即可压缩出一个该文件的压缩文件，后缀为.lcp

decompress + 空格 + 文件名(带后缀.lcp) 即可解压出一个去掉后缀(.lcp)的文件

copyright: lwher 